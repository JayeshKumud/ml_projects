# ------------------------------------------------------------
# REQUIREMENTS.TXT — WHY THIS FILE IS NEEDED
# ------------------------------------------------------------
# This file defines all Python dependencies required to run the
# Home Price Prediction Flask API. Docker and deployment tools
# use requirements.txt to install the exact versions of packages
# needed for the application to work consistently across machines.
#
# WHY WE NEED THIS FILE:
#   • Ensures reproducible environments (same versions everywhere)
#   • Prevents version conflicts between NumPy, Flask, scikit‑learn
#   • Allows Docker to install dependencies automatically
#   • Makes deployment predictable and stable
#   • Helps other developers set up the project quickly
#
# WHAT THIS FILE DOES:
#   • Lists only the packages required by the backend API
#   • Ensures the ML model loads correctly inside Docker
#   • Keeps the Docker image small by avoiding unnecessary packages
#
# Without requirements.txt, the application may fail due to
# missing libraries, incompatible versions, or environment drift.
# ------------------------------------------------------------
Flask==3.1.3
numpy==1.26.4
scikit-learn==1.6.0
faiss-cpu==1.7.4
transformers==4.47.1
datasets==3.2.0
evaluate==0.4.3
accelerate==1.2.1
sentencepiece==0.2.0
tqdm==4.68.3
clean-text==0.6.0
seaborn==0.13.2
matplotlib==3.10.0
pandas==2.0.0
